-- CreateTable
CREATE TABLE "Javali" (
    "id" INTEGER NOT NULL,
    "nome" TEXT NOT NULL,
    "sexo" VARCHAR(255) NOT NULL,
    "adulto" BOOLEAN NOT NULL,

    CONSTRAINT "Javali_pkey" PRIMARY KEY ("id")
);
