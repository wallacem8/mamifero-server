-- CreateTable
CREATE TABLE "Javali" (
    "nome" TEXT NOT NULL,
    "peso" INTEGER NOT NULL,
    "sexo" VARCHAR(255) NOT NULL,
    "adulto" BOOLEAN NOT NULL,

    CONSTRAINT "Javali_pkey" PRIMARY KEY ("nome")
);
