generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Javali {
  nome String @id
  peso Int
  sexo String @db.VarChar(255)
  adulto Boolean
}